# Config file for `media-magic.sh`.
# Easy setting of defaults, without having to dig around in the script itself.

# Where to mount the cdrom for DVD ripping (vobcopy requires mounted):
DVD_MOUNTPOINT='/mnt/cdrom'

# Directory for the CD output. Ensure this matches the OUTPUTDIR in abcde.conf
# on line 393.
OUTPUT_CDS='/media/media/CDs'

#  Base path of raw DVD rips. Creates this:
#   $OUTPUT_DVDS/
#     ├─ processing/   (while ripping)
#     ├─ hash/         (when completed)
#     └─ name/         (linked here)
OUTPUT_DVDS='/media/media/DVDs/raw'
